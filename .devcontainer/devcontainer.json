{
  "name": "Node.js Development Environment",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:14",
  "forwardPorts": [
    3000,
    8545
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
          "source.organizeImports": true
        },
        "javascript.validate.enable": false
      },
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker",
        "oderwat.indent-rainbow"
      ]
    }
  },
  "postCreateCommand": [
    "curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash && . ~/.nvm/nvm.sh && nvm install 16 && nvm use 16 && npx jacob-setup create && nvm use 14 && yarn install --frozen-lockfile --ignore-engines"
  ]
}
